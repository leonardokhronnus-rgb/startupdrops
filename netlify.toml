[build]
  publish = "."
  functions = "netlify/functions"

# O schedule também está inline em reescrever.mjs (config.schedule).
# Deixado aqui como documentação/reforço.
[functions."reescrever"]
  schedule = "0 11,17,23 * * *"
